let initialState = {
  namesUsers: [
    { id: 1, name: "Kolya " },
    { id: 2, name: "Misha " },
    { id: 3, name: "Sveta " },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
